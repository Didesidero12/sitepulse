[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Disable Netlify's secrets scanner — our Firebase/Mapbox keys are public
SECRETS_SCAN_SMART_DETECTION_ENABLED = "false"