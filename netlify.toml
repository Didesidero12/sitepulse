[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# DISABLE SECRETS SCANNING — public Firebase/Mapbox tokens are safe
SECRETS_SCAN_SMART_DETECTION_ENABLED = "false"