[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Disable secrets scanner — Firebase/Mapbox public keys are safe
SECRETS_SCAN_SMART_DETECTION_ENABLED = "false"